<%-include('../partials/head.ejs') %>
<body>
    <%-include('../partials/header.ejs') %>
    <main>
        <!-- <section class="main_caja_hija_detalle_compra">
            <article class="fondo_de_articulo">
                    <img class="img_producto_de_compra" src="/img/estabilizador 1.png" alt="Producto" width="100%" height="20%">
                    <p class="carrito_p-detalle-producto">Estabilizador De Tensión Trv - Micro Volt L - 1200va(pico) F
                    </p>
                    <h3 class="carrito_h3-precio">$ 50.000</h3>
                <div class="div_contador">
                    <div class="div_contador_icons">
                        <div>
                            <button class="boton_sumador"><i class="fa-solid fa-plus"></i></button>
                            <input type="number" name="contador_del_producto" min="0" max="1000" class="imput_contador">
                            <button class="boton_de_resta"><i class="fa-solid fa-minus"></i></button>
                        </div>
                        <i class="fa-solid fa-trash icon_tacho"></i>
                    </div>
                    <h3 class="precio"> $ 50.000</h3>
                </div>     
            </article> -->
        <form method="POST" action="products/formUpdate" id="" class="form_create">
            <% for (let x = 0; x < formRegistro.length; x++) { %>
                
                    <label class="label-create" for="<%= formRegistro[x].toLowerCase() %>"><%= formRegistro[x] %></label>
                    <% switch (formRegistro[x]) { 
                        case "descripcion": %>
                            <textarea class="textarea-create" name="<%= formRegistro[x].toLowerCase() %>" id="<%= formRegistro[x].toLowerCase() %>" cols="30" rows="10" placeholder=" Ingrese el detalle del producto"></textarea>
                            <% break;
                        case "imagen": %>
                            <input class="image-create" type="file" name="<%= formRegistro[x].toLowerCase() %>" id="<%= formRegistro[x].toLowerCase() %>" accept="image/*" >
                            <% break;
                        case "id": %>
                            <input class="id-create" type="number" name="<%= formRegistro[x].toLowerCase() %>" placeholder="<%= formRegistro[x] %>">
                            <% break;
                        default: %>
                            <input class="text-create" type="text" name="<%= formRegistro[x].toLowerCase() %>" placeholder="<%= formRegistro[x] %>">
                    <% } %>
            <% } %>       
        </form>
        <div class="boton_registro_update ">
                    <a href="/products/dashboard">
                            <button type="submit">Editar Producto</button>
                    </a>
                    <a href="/dashboard">
                            <button type="submit">Eliminar </button>
                    </a>   
        </div>  
        
    </main>
    <%-include('../partials/footer.ejs') %>
</body>
</html>   